name: Cart Deployment
run-name: ${{ github.actor }} Cart Deployment 🚀
on: [push]
jobs:
  DEV:
    runs-on: self-hosted
    steps:
      - name: Checkout helm repo
        uses: actions/checkout@v4
        with:
          repository: VenkatBheemireddyOrg/roboshop-helm
      - run: ls -l
        name: Deployment
      - run: echo DEV

  QA:
    runs-on: self-hosted
    needs: DEV
    steps:
      - run: echo QA

  UAT:
    runs-on: self-hosted
    needs: QA
    steps:
      - run: echo UAT

  PROD:
    runs-on: self-hosted
    needs: UAT
    steps:
      - run: echo PROD